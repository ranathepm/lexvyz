# Drop the demo call audio here

Put your AI intake call recording in this folder and name it:

    lexvyz-demo-call.mp3

(Or update `AUDIO_SRC` in `components/landing/audio-demo-player.tsx`
to point at whatever you name it.)
